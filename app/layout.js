import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Procope AI — AI Automation Services",
  description:
    "Procope AI provides AI Automation for Real Estate, Medical Industry, Hospitality, Hotels, and Restaurants. Services: AI Agents, AI Call Assistants, AI Chatbots, Meta and WhatsApp Automation, and custom AI solutions.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
        suppressHydrationWarning={true}
      >
        {children}
      </body>
    </html>
  );
}
